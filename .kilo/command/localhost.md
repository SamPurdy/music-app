{
  "env": {
    "SHELL": "/bin/bash"
  },
  "commands": {
    "default": {
      "type": "bash",
      "description": "Default command runner using bash"
    }
  },
  "scripts": {
    "dev": ["npm run dev"],
    "build": ["npm run build"],
    "check": ["npm run lint"],
    "format": ["npm run format"]
  }
}